import { humanDate } from 'webui/helpers/human-date';
import { module, test } from 'qunit';

module('Unit | Helper | human date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = humanDate([42]);
  assert.ok(result);
});
