import { mime } from 'webui/helpers/mime';
import { module, test } from 'qunit';

module('Unit | Helper | mime');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mime([42]);
  assert.ok(result);
});
