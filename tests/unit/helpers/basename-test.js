import { basename } from 'webui/helpers/basename';
import { module, test } from 'qunit';

module('Unit | Helper | basename');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = basename([42]);
  assert.ok(result);
});
