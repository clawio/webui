import { humanSize } from 'webui/helpers/human-size';
import { module, test } from 'qunit';

module('Unit | Helper | human size');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = humanSize([42]);
  assert.ok(result);
});
