import { timeSince } from 'webui/helpers/time-since';
import { module, test } from 'qunit';

module('Unit | Helper | time since');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timeSince([42]);
  assert.ok(result);
});
