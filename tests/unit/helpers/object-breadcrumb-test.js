import { objectBreadcrumb } from 'webui/helpers/object-breadcrumb';
import { module, test } from 'qunit';

module('Unit | Helper | object breadcrumb');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = objectBreadcrumb([42]);
  assert.ok(result);
});
