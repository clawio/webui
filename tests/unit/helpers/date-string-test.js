import { dateString } from 'webui/helpers/date-string';
import { module, test } from 'qunit';

module('Unit | Helper | date string');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateString([42]);
  assert.ok(result);
});
